BEGIN;

-- Enums
CREATE TYPE UserRole AS ENUM ('admin', 'serviceleitung', 'service');
CREATE TYPE EntityStatus AS ENUM ('active', 'inactive', 'deleted');

-- Users
CREATE TABLE IF NOT EXISTS users (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name TEXT NOT NULL,
    username TEXT UNIQUE NOT NULL,
    password_hash TEXT NULL,
    onetime_password_hash TEXT NULL,
    role UserRole NOT NULL,
    status EntityStatus NOT NULL ,
    created_at TIMESTAMPTZ NOT NULL,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_users_username ON users(username);
CREATE INDEX IF NOT EXISTS idx_users_status ON users(status);

COMMENT ON TABLE users IS 'System users who perform actions in jotti';
COMMENT ON COLUMN users.name IS 'Full name of the user';
COMMENT ON COLUMN users.username IS 'Unique username';
COMMENT ON COLUMN users.password_hash IS 'Argon2id password hash; NULL if user has not set a password (on first login)';
COMMENT ON COLUMN users.onetime_password_hash IS 'Argon2id password hash for a one-time password. That can be used to setup a new user or reset their password as an admin. NULL if not set';
COMMENT ON COLUMN users.role IS 'Role of the user, determining access rights';
COMMENT ON COLUMN users.status IS 'Account status: active, inactive, or deleted';
COMMENT ON COLUMN users.created_at IS 'Creation timestamp (UTC)';
COMMENT ON COLUMN users.updated_at IS 'Last modification timestamp (UTC)';

-- Create first admin user (without onetime password)
INSERT INTO users (username, name, role, onetime_password_hash, status, created_at) VALUES (
  'nico', 'Nico Gräf', 'admin', '$argon2id$v=19$m=64,t=2,p=2$ekV4Uzg2cUhVTTBUaTJJVw$4Sfsc6eRVIWXSzgNoWaybDBws3c830yC6IMcdUDG1ns', 'active', now()
);

-- Tische
CREATE TABLE IF NOT EXISTS tische (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name TEXT UNIQUE NOT NULL,
    status EntityStatus NOT NULL,
    created_at TIMESTAMPTZ NOT NULL,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_tische_status ON tische(status);

COMMENT ON TABLE tische IS 'Gäste sitzen an Tischen und geben dort Bestellungen auf.';
COMMENT ON COLUMN tische.name IS 'Name oder Nummer des Tisches (z.B. "Tisch 1")';
COMMENT ON COLUMN tische.status IS 'Tisch-Status: active, inactive oder deleted';
COMMENT ON COLUMN tische.created_at IS 'Creation timestamp (UTC)';
COMMENT ON COLUMN tische.updated_at IS 'Last modification timestamp (UTC)';

-- Produkte
CREATE TYPE ProduktKategorie AS ENUM ('essen', 'getraenk', 'sonstiges');

CREATE TABLE IF NOT EXISTS produkte (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name TEXT UNIQUE NOT NULL,
    kategorie ProduktKategorie NOT NULL,
    status EntityStatus NOT NULL DEFAULT 'active',
    created_at TIMESTAMPTZ NOT NULL,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE produkte IS 'Produkte, die von Gästen bestellt werden können.';
COMMENT ON COLUMN produkte.name IS 'Name des Produkts';
COMMENT ON COLUMN produkte.kategorie IS 'Kategorie des Produkts: essen, getraenk oder sonstiges';
COMMENT ON COLUMN produkte.status IS 'Produkt-Status: active, inactive oder deleted';
COMMENT ON COLUMN produkte.created_at IS 'Creation timestamp (UTC)';
COMMENT ON COLUMN produkte.updated_at IS 'Last modification timestamp (UTC)';


-- Produkt-Varianten

CREATE TABLE IF NOT EXISTS produkt_varianten (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    produkt_id INT REFERENCES produkte(id) NOT NULL,
    name TEXT NOT NULL,
    preis_cents INT NOT NULL,
    status EntityStatus NOT NULL,
    created_at TIMESTAMPTZ NOT NULL,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_produkt_varianten_status ON produkt_varianten(status);

COMMENT ON TABLE produkt_varianten IS 'Varianten von Produkten (verschiedene Größen, Optionen, etc.).';
COMMENT ON COLUMN produkt_varianten.name IS 'Name der Produktvariante';
COMMENT ON COLUMN produkt_varianten.preis_cents IS 'Preis in Cent (z.B. 199 für 1,99 €)';
COMMENT ON COLUMN produkt_varianten.status IS 'Varianten-Status: active, inactive oder deleted';
COMMENT ON COLUMN produkt_varianten.created_at IS 'Creation timestamp (UTC)';
COMMENT ON COLUMN produkt_varianten.updated_at IS 'Last modification timestamp (UTC)';


-- ============================================================
-- Table: kassensitzungen (CRUD entity for Kassensitzung lifecycle)
-- ============================================================
CREATE TABLE kassensitzungen (
    z_nr INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    datum DATE NOT NULL,
    bezeichnung TEXT NOT NULL DEFAULT '',
    status TEXT NOT NULL CHECK (status IN ('offen', 'abgeschlossen')),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE kassensitzungen IS 'CRUD entity for Kassensitzung lifecycle. Each row represents one Kassensitzung (one Betriebstag). The z_nr is the DSFinV-K-compliant sequential number.';
COMMENT ON COLUMN kassensitzungen.z_nr IS 'Fortlaufende Kassensitzungsnummer (Z_NR), DSFinV-K-Pflichtfeld';
COMMENT ON COLUMN kassensitzungen.datum IS 'Betriebstag der Kassensitzung';
COMMENT ON COLUMN kassensitzungen.bezeichnung IS 'Optionale Bezeichnung (z.B. "Sommerfest Tag 1")';
COMMENT ON COLUMN kassensitzungen.status IS 'Kassensitzung-Status: offen oder abgeschlossen';
COMMENT ON COLUMN kassensitzungen.created_at IS 'Creation timestamp (UTC)';
COMMENT ON COLUMN kassensitzungen.updated_at IS 'Last modification timestamp (UTC)';

-- ============================================================
-- Table: kassenjournal (Event Store for all kasse operations)
-- ============================================================
-- Append-only, never updated or deleted (INSERT and SELECT only).
CREATE TABLE IF NOT EXISTS kassenjournal (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id INT REFERENCES users(id) NOT NULL,
    user_name TEXT NOT NULL,
    type TEXT NOT NULL,
    subject TEXT NOT NULL,
    version INT NOT NULL,
    timestamp TIMESTAMPTZ NOT NULL,
    data JSONB NOT NULL,
    kassensitzung_nr INT NOT NULL REFERENCES kassensitzungen(z_nr),
    UNIQUE (subject, version)
);

CREATE INDEX IF NOT EXISTS idx_kassenjournal_user_id ON kassenjournal(user_id);
CREATE INDEX IF NOT EXISTS idx_kassenjournal_subject ON kassenjournal(subject);
CREATE INDEX IF NOT EXISTS idx_kassenjournal_type ON kassenjournal(type);
CREATE INDEX IF NOT EXISTS idx_kassenjournal_subject_type ON kassenjournal(subject, type);
CREATE INDEX IF NOT EXISTS idx_kassenjournal_type_timestamp ON kassenjournal(type, timestamp);
CREATE INDEX IF NOT EXISTS idx_kassenjournal_ks_nr ON kassenjournal(kassensitzung_nr);

-- Restrict public role to SELECT + INSERT only (defense-in-depth for non-owner roles)
REVOKE ALL ON TABLE kassenjournal FROM PUBLIC;
GRANT SELECT, INSERT ON TABLE kassenjournal TO PUBLIC;

-- Triggers to enforce append-only immutability for ALL roles, including the table owner.
-- REVOKE/GRANT alone is not sufficient because PostgreSQL table owners bypass privilege checks.
CREATE OR REPLACE FUNCTION prevent_kassenjournal_mutation() RETURNS TRIGGER AS $$
BEGIN
    RAISE EXCEPTION 'kassenjournal table is append-only: % not allowed', TG_OP;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER kassenjournal_no_update
    BEFORE UPDATE ON kassenjournal FOR EACH ROW
    EXECUTE FUNCTION prevent_kassenjournal_mutation();

CREATE TRIGGER kassenjournal_no_delete
    BEFORE DELETE ON kassenjournal FOR EACH ROW
    EXECUTE FUNCTION prevent_kassenjournal_mutation();

CREATE TRIGGER kassenjournal_no_truncate
    BEFORE TRUNCATE ON kassenjournal FOR EACH STATEMENT
    EXECUTE FUNCTION prevent_kassenjournal_mutation();

COMMENT ON TABLE kassenjournal IS 'Append-only Kassenjournal (Event Store) for all kasse operations (event-sourcing)';
COMMENT ON COLUMN kassenjournal.user_id IS 'Actor who triggered the event';
COMMENT ON COLUMN kassenjournal.user_name IS 'Display name of the actor at the time of the event';
COMMENT ON COLUMN kassenjournal.version IS 'Optimistic concurrency version per subject (monotonically increasing)';
COMMENT ON COLUMN kassenjournal.type IS 'Event type identifier, e.g., bestellung-aufgenommen:v1';
COMMENT ON COLUMN kassenjournal.subject IS 'Aggregate key, e.g. "kassensitzung-1/tisch-42" for a tisch session, "kassensitzung-1" for a kassensitzung';
COMMENT ON COLUMN kassenjournal.timestamp IS 'Event time (UTC)';
COMMENT ON COLUMN kassenjournal.data IS 'Event data (jsonb), versioned by type';
COMMENT ON COLUMN kassenjournal.kassensitzung_nr IS 'Kassensitzung number (z_nr) this event belongs to';

-- ============================================================
-- Table: tisch_sessions (CQRS projection of per-tisch-session state)
-- ============================================================
CREATE TABLE tisch_sessions (
    subject TEXT PRIMARY KEY,
    tisch_id INT NOT NULL REFERENCES tische(id),
    kassensitzung_nr INT NOT NULL REFERENCES kassensitzungen(z_nr),
    saldo_cents INTEGER NOT NULL DEFAULT 0,
    unbezahlte_positionen JSONB NOT NULL DEFAULT '[]'::jsonb,
    ausstehende_positionen JSONB NOT NULL DEFAULT '[]'::jsonb,
    gesamt_zahlungen_cents INTEGER NOT NULL DEFAULT 0,
    last_event_id INTEGER NOT NULL REFERENCES kassenjournal(id),
    last_event_version INTEGER NOT NULL,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_tisch_sessions_ks_nr ON tisch_sessions(kassensitzung_nr);

COMMENT ON TABLE tisch_sessions IS 'Synchronous CQRS projection of per-tisch-session state, session-scoped (PK: subject), updated within the event-write transaction';

-- ============================================================
-- Table: tisch_favoriten (per-user favorite tables)
-- ============================================================
CREATE TABLE tisch_favoriten (
    user_id INT REFERENCES users(id) NOT NULL,
    tisch_id INT REFERENCES tische(id) NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (user_id, tisch_id)
);

CREATE INDEX IF NOT EXISTS idx_tisch_favoriten_user_id ON tisch_favoriten(user_id);

COMMENT ON TABLE tisch_favoriten IS 'Per-user favourite tables; each service user can mark tables they are responsible for.';

-- ============================================================
-- Table: kategorie_drucker (Drucker-Konfiguration pro Produkt-Kategorie)
-- ============================================================
CREATE TABLE kategorie_drucker (
    kategorie   ProduktKategorie PRIMARY KEY,
    drucker_ip  VARCHAR(50) NOT NULL DEFAULT '',
    bonmodus    TEXT NOT NULL DEFAULT 'pro_position'
                CHECK (bonmodus IN ('pro_position', 'pro_bestellung')),
    updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE kategorie_drucker IS 'Drucker-IP und Bonmodus pro Produkt-Kategorie für den Bondruck.';
COMMENT ON COLUMN kategorie_drucker.drucker_ip IS 'IPv4-Adresse des Bondruckers (leer = kein Drucker konfiguriert)';
COMMENT ON COLUMN kategorie_drucker.bonmodus IS 'Bonmodus: pro_position (1 Bon pro Position) oder pro_bestellung (1 Sammelbon)';

INSERT INTO kategorie_drucker (kategorie) VALUES ('essen'), ('getraenk'), ('sonstiges');

COMMIT;