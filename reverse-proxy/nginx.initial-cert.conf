# Catch-all HTTP server for initial Let's Encrypt certificate issuance.
# server_name _ accepts requests for any domain pointing to this server,
# so self-hosters don't need to edit this file.
server {
  listen 80;
  server_name _;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }
}
