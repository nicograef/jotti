# scripts/check-domain-enums.sh allowlist — one exception per line: the tracked
# path, a space-free code fragment matched as a substring of the offending line,
# then "# " and the reason. An entry that matches nothing turns the gate red.

backend/internal/tools/checklanguage/main.go "getraenk": #Rechtschreib-Korrekturkarte (Wortstamm -> Umlaut-Form), keine Kategorie
