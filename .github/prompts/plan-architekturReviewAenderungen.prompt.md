## Plan: §7 Architektur-Review — kassensitzung_state → kassensitzungen + Subject-Format

### Hintergrund & Kontext

`docs/redesign.md` §7 begründet zwei architektonische Änderungen am bestehenden Kassenjournal-Redesign:

**Änderung A — `kassensitzung_state` wird zur `kassensitzungen` CRUD-Entität:**
Das bisherige Design (§3.8 redesign.md) definiert `kassensitzung_state` als "synchrone CQRS-Projektion" — analog zu `tisch_session_state`. Aber `kassensitzung_state` erfüllt faktisch zwei Rollen: (1) Hot-Path-Read für die KS-Sperre und (2) CRUD-artige Stammdaten-Entität (Kassensitzungs-Verwaltung). Anders als `tisch_session_state` (die aus Events rekonstruierbar sein _muss_) ist `kassensitzung_state` die Single Source of Truth — sie wird nie aus Events replayed. Daher: Umwandlung zur CRUD-Entität `kassensitzungen` ohne `subject`, `last_event_id`, `last_event_version`.

**Änderung B — Subject-Format:**
Altes Format: `kassensitzung-{YYYYMMDD}-tisch-{id}` (z.B. `kassensitzung-20260322-tisch-42`)
Neues Format: `kassensitzung-{nr}/tisch-{id}` (z.B. `kassensitzung-1/tisch-42`)
KS-Subject: `kassensitzung-{YYYYMMDD}` → `kassensitzung-{nr}` (z.B. `kassensitzung-1`)
Der `/`-Separator macht die Hierarchie explizit (KS-Segment / Tisch-Segment).

**Was sich NICHT ändert:**

- `tisch_session_state` bleibt eine CQRS-Projektion (mit `last_event_id`, `last_event_version`, PK `subject`)
- `docs/redesign.md` §7 selbst wird NICHT verändert — es dokumentiert die Entscheidung
- `database/seed.sql` ist NICHT betroffen — sie nutzt den alten `events`-Tabellennamen (Pre-Redesign) und enthält keine `kassensitzung_state`-Referenzen
- `backend/sqlc/dbgen/` wird NICHT manuell editiert — es wird durch `make sqlc` neu generiert
- Keine Hand-geschriebenen Go-Dateien referenzieren `KassensitzungState` oder die alten Queries (nur der generierte Code in `sqlc/dbgen/`)

---

### Phase 1: Code-Änderungen (Schema + Queries)

#### Schritt 1: DB-Schema anpassen

**Datei:** `database/migrations/01_initial.up.sql`

**1a) Neue `kassensitzungen`-Tabelle einfügen — direkt VOR dem `kassenjournal`-Block.**

Füge den folgenden Block ein, direkt VOR der Zeile `-- ============================================================` / `-- Table: kassenjournal`:

```sql
-- ============================================================
-- Table: kassensitzungen (CRUD entity for Kassensitzung lifecycle)
-- ============================================================
CREATE TABLE kassensitzungen (
    z_nr INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    datum DATE NOT NULL,
    bezeichnung TEXT NOT NULL DEFAULT '',
    status TEXT NOT NULL CHECK (status IN ('offen', 'abgeschlossen')),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE kassensitzungen IS 'CRUD entity for Kassensitzung lifecycle. Each row represents one Kassensitzung (one Betriebstag). The z_nr is the DSFinV-K-compliant sequential number.';
COMMENT ON COLUMN kassensitzungen.z_nr IS 'Fortlaufende Kassensitzungsnummer (Z_NR), DSFinV-K-Pflichtfeld';
COMMENT ON COLUMN kassensitzungen.datum IS 'Betriebstag der Kassensitzung';
COMMENT ON COLUMN kassensitzungen.bezeichnung IS 'Optionale Bezeichnung (z.B. "Sommerfest Tag 1")';
COMMENT ON COLUMN kassensitzungen.status IS 'Kassensitzung-Status: offen oder abgeschlossen';
COMMENT ON COLUMN kassensitzungen.created_at IS 'Creation timestamp (UTC)';
COMMENT ON COLUMN kassensitzungen.updated_at IS 'Last modification timestamp (UTC)';

```

**1b) FK auf `kassenjournal.kassensitzung_nr` hinzufügen.**

In der `kassenjournal`-CREATE-TABLE-Definition die Zeile:

```
    kassensitzung_nr INT NOT NULL,
```

ersetzen durch:

```
    kassensitzung_nr INT NOT NULL REFERENCES kassensitzungen(z_nr),
```

**1c) FK auf `tisch_session_state.kassensitzung_nr` hinzufügen.**

In der `tisch_session_state`-CREATE-TABLE-Definition die Zeile:

```
    kassensitzung_nr INT NOT NULL,
```

ersetzen durch:

```
    kassensitzung_nr INT NOT NULL REFERENCES kassensitzungen(z_nr),
```

**1d) Subject-Kommentar auf `kassenjournal.subject` aktualisieren.**

Die Zeile:

```
COMMENT ON COLUMN kassenjournal.subject IS 'Aggregate key, e.g. "kassensitzung-20260322-tisch-42" for a tisch session';
```

ersetzen durch:

```
COMMENT ON COLUMN kassenjournal.subject IS 'Aggregate key, e.g. "kassensitzung-1/tisch-42" for a tisch session, "kassensitzung-1" for a kassensitzung';
```

**1e) Alten `kassensitzung_state`-Block komplett entfernen.**

Folgenden gesamten Block löschen (suche nach `-- Table: kassensitzung_state`):

```sql
-- ============================================================
-- Table: kassensitzung_state (CQRS projection of kassensitzung state)
-- ============================================================
CREATE TABLE kassensitzung_state (
    subject TEXT PRIMARY KEY,
    z_nr INT UNIQUE NOT NULL,
    datum DATE NOT NULL,
    status TEXT NOT NULL CHECK (status IN ('offen', 'abgeschlossen')),
    last_event_id INT NOT NULL REFERENCES kassenjournal(id),
    last_event_version INT NOT NULL
);

COMMENT ON TABLE kassensitzung_state IS 'Synchronous CQRS projection of kassensitzung state (hot-path: status + z_nr), updated within the event-write transaction';
```

#### Schritt 2: SQL-Queries anpassen

**2a) Datei löschen:** `backend/sqlc/queries/kassensitzung_state.sql`

```bash
rm backend/sqlc/queries/kassensitzung_state.sql
```

**2b) Neue Datei erstellen:** `backend/sqlc/queries/kassensitzungen.sql`

Exakter Inhalt:

```sql
-- name: InsertKassensitzung :one
INSERT INTO kassensitzungen (datum, bezeichnung, status, created_at, updated_at)
VALUES ($1, $2, $3, NOW(), NOW()) RETURNING z_nr;

-- name: UpdateKassensitzungStatus :exec
UPDATE kassensitzungen SET status = $2, updated_at = NOW() WHERE z_nr = $1;

-- name: GetOffeneKassensitzung :one
SELECT z_nr, datum, bezeichnung, status, created_at, updated_at
FROM kassensitzungen WHERE status = 'offen' LIMIT 1;

-- name: GetKassensitzungByZNr :one
SELECT z_nr, datum, bezeichnung, status, created_at, updated_at
FROM kassensitzungen WHERE z_nr = $1;

-- name: GetNextZNr :one
SELECT COALESCE(MAX(z_nr), 0) + 1 AS next_z_nr FROM kassensitzungen;

-- name: DeleteAllKassensitzungen :exec
DELETE FROM kassensitzungen;
```

**2c) sqlc neu generieren:**

```bash
make sqlc
```

Dieser Befehl löscht den alten generierten Code (`backend/sqlc/dbgen/kassensitzung_state.sql.go`) und erzeugt `backend/sqlc/dbgen/kassensitzungen.sql.go` + aktualisiertes `models.go`.

---

### Phase 2: Dokumentation aktualisieren

Alle Schritte in Phase 2 sind voneinander unabhängig und können parallel bearbeitet werden. Lies jeden Abschnitt der jeweiligen Datei vor dem Editieren.

**Allgemeine Ersetzungsregeln** (gelten für ALLE Dokumente in Phase 2, außer `docs/redesign.md` §7):

| Alt                                                   | Neu                                                                                         | Kontext                                             |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| `kassensitzung_state` (Tabellenname)                  | `kassensitzungen`                                                                           | Überall wo es den DB-Tabellennamen meint            |
| `kassensitzung_state`-Projektion / KS-Projektion      | `kassensitzungen`-Entität / KS-Entität                                                      | Beschreibende Texte                                 |
| `Projektion` (wenn Kassensitzung gemeint)             | `CRUD-Entität`                                                                              | Kassensitzung ist keine Projektion mehr             |
| `Zwei synchrone Projektionen`                         | `Eine synchrone Projektion (`tisch_session_state`) + eine CRUD-Entität (`kassensitzungen`)` | Wo beide zusammen beschrieben werden                |
| `kassensitzung-{YYYYMMDD}-tisch-{id}`                 | `kassensitzung-{nr}/tisch-{id}`                                                             | Subject-Format Tisch-Session                        |
| `kassensitzung-{YYYYMMDD}`                            | `kassensitzung-{nr}`                                                                        | Subject-Format Kassensitzung                        |
| `kassensitzung-20260322-tisch-42`                     | `kassensitzung-1/tisch-42`                                                                  | Konkretes Beispiel                                  |
| `kassensitzung-20260322-tisch-43`                     | `kassensitzung-1/tisch-43`                                                                  | Konkretes Beispiel                                  |
| `kassensitzung-20260322-tisch-7`                      | `kassensitzung-1/tisch-7`                                                                   | Konkretes Beispiel                                  |
| `kassensitzung-20260322`                              | `kassensitzung-1`                                                                           | KS-Subject-Beispiel (Tag 1)                         |
| `kassensitzung-20260323`                              | `kassensitzung-2`                                                                           | KS-Subject-Beispiel (Tag 2)                         |
| `kassensitzung-20260501-tisch-42`                     | `kassensitzung-1/tisch-42`                                                                  | Beispiel in Instructions                            |
| `kassensitzung-20260501`                              | `kassensitzung-1`                                                                           | Beispiel in Instructions                            |
| `-tisch-` (im Subject-Kontext)                        | `/tisch-`                                                                                   | Separator-Änderung                                  |
| `LIKE 'kassensitzung-20260322%'`                      | `LIKE 'kassensitzung-1/%'` oder `WHERE kassensitzung_nr = 1`                                | Query-Beispiele                                     |
| `UPSERT kassensitzung_state`                          | `INSERT INTO kassensitzungen` / `UPDATE kassensitzungen`                                    | Write-Flow-Pseudocode                               |
| `last_event_id`, `last_event_version` (im KS-Kontext) | entfernen                                                                                   | Kassensitzung hat keine Event-Tracking-Spalten mehr |
| `subject TEXT PRIMARY KEY` (im KS-Schema)             | `z_nr INT ... PRIMARY KEY`                                                                  | Schema-Darstellungen                                |

**ACHTUNG:** `docs/redesign.md` §7 (ab ~Zeile 930) wird NICHT verändert — es ist die Begründung dieser Änderungen.

#### Schritt 3: `docs/redesign.md` §3.1–§3.13 aktualisieren

Lies `docs/redesign.md` vollständig (große Datei, ~1000 Zeilen). Wende die Ersetzungsregeln auf §3.1–§3.13 (ca. Zeile 95–850) an. Besonders aufwendig:

- **§3.1 Mermaid-Diagramm** (~L107-111): `kassensitzung_state` → `kassensitzungen`, Subject-Beispiele
- **§3.3** (~L207): FK-Kommentar ergänzen: `kassensitzung_nr` → `kassensitzungen(z_nr)`
- **§3.4 Subject-Format** (~L219-300): **Komplett umschreiben.** Alle `{YYYYMMDD}`-Referenzen → `{nr}`. Alle `-tisch-` → `/tisch-`. "Warum Datum?" Abschnitt → "Warum z_nr?" umformulieren (z_nr ist selbstdokumentierend da fortlaufend + FK-fähig). Query-Beispiele mit `LIKE` anpassen. Mermaid-Diagramm aktualisieren.
- **§3.6** (~L335-380): "Datum bestimmt das Subject" umformulieren → z_nr bestimmt das Subject
- **§3.7 Invarianten** (~L443-490): `kassensitzung_state` → `kassensitzungen`, Write-Flow-Beschreibungen
- **§3.8 Projektionen** (~L500-590): `kassensitzung_state`-Abschnitt **komplett umschreiben** zu `kassensitzungen`-CRUD-Entität. Schema-Sketch anpassen (kein `subject`, kein `last_event_id`, kein `last_event_version`). Write-Through-Pseudocode: UPSERT → INSERT/UPDATE. Selbstheilung: `kassensitzungen` rausnehmen (keine replay-fähige Projektion mehr).
- **§3.10 DSFinV-K** (~L658): `kassensitzung_state.z_nr` → `kassensitzungen.z_nr`
- **§3.11 Sequence Diagram** (~L685-724): Alle Subject-Beispiele + Projektions-Beschriftungen
- **§3.13 Auswirkungstabelle** (~L778-842): Tabel-Zeilen + Summentext

#### Schritt 4: `docs/handbuch.md` aktualisieren (~20 Stellen)

Wende Ersetzungsregeln an. Hauptbereiche:

- §3.2 Subject-Format + Beispiele
- §3.7 KS-Sperre: `kassensitzung_state-Projektion` → `kassensitzungen-Entität`
- §3.8 Projektionen: Routing-Tabelle, Write-Through-Beschreibung, Spalten-Tabelle (KS-Schema ohne subject/last_event_id/last_event_version), Selbstheilung (kassensitzungen rausnehmen)
- §7 Read Models: "synchronen Projektionen" → "Projektion + Entität"

#### Schritt 5: `docs/language.md` aktualisieren (~8 Stellen)

- Tabellenname: `kassensitzung_state` → `kassensitzungen`
- Subject-Format-Referenzen
- Kassensitzung-Definitionsabschnitt

#### Schritt 6: `docs/anforderungen.md` aktualisieren (3 Stellen)

- "Zwei synchrone Projektionen" → "Eine Projektion + eine CRUD-Entität"
- Subject-Format-Beispiele: `kassensitzung-{nr}/tisch-{id}`

#### Schritt 7: `docs/diagrams.md` aktualisieren (~20 Stellen)

- Alle Mermaid + ASCII-Diagramme: `kassensitzung_state` → `kassensitzungen`
- ER-Diagramm: komplett umbauen (neues Schema, FK-Beziehungen)
- Subject-Beispiele

#### Schritt 8: ADRs aktualisieren

- **`docs/adr/event-sourcing.md`** (~15 Stellen): Projektions-Beschreibungen, Schema-Sketch, Subject-Beispiele
- **`docs/adr/cqrs.md`** (~12 Stellen): Projektionstabellen-Referenzen, KS-Sperre-Beschreibung

#### Schritt 9: `docs/roadmap.md` aktualisieren (1 Stelle)

- `kassensitzung_state` → `kassensitzungen (CRUD-Entität)`

#### Schritt 10: Instruktionen aktualisieren

**`.github/instructions/event-sourcing.instructions.md`** (~6 Stellen):

- Subject-Formate: `kassensitzung-{YYYYMMDD}-tisch-{id}` → `kassensitzung-{nr}/tisch-{id}`, `kassensitzung-{YYYYMMDD}` → `kassensitzung-{nr}`
- Konkrete Beispiele: `kassensitzung-20260501-tisch-42` → `kassensitzung-1/tisch-42`, `kassensitzung-20260501` → `kassensitzung-1`
- "zwei synchrone Projektionen" → "eine synchrone Projektion (`tisch_session_state`) + eine CRUD-Entität (`kassensitzungen`)"
- Event-Store-Abschnitt: `kassensitzung_state — Kassensitzung-Projektion` → `kassensitzungen — Kassensitzung-Entität (CRUD)`

**`.github/instructions/database.instructions.md`** (1 Stelle):

- Die Zeile `Kasse-Tabellen: ..., kassensitzung_state (Kassensitzung-Projektion).` → `..., kassensitzungen (Kassensitzung-Entität, CRUD).`

#### Schritt 11: AGENTS.md + README.md aktualisieren

**`AGENTS.md`** (2 Stellen):

- Suche `Zwei Synchrone Projektionen (tisch_session_state, kassensitzung_state)` → `Synchrone Projektion (tisch_session_state), CRUD-Entität (kassensitzungen)`
- Suche `Zwei synchrone Projektionen (\`tisch_session_state\`, \`kassensitzung_state\`)`→`Eine synchrone Projektion (\`tisch_session_state\`) und eine CRUD-Entität (\`kassensitzungen\`)`

**`README.md`** (1 Stelle):

- Suche `Zwei synchrone Projektionen (\`tisch_session_state\`, \`kassensitzung_state\`)` → entsprechend anpassen

---

### Phase 3: Plan- und Analyse-Datei aktualisieren

#### Schritt 12: `docs/agents/kassenjournal-redesign/plan.md` aktualisieren

- Neue Redo-Abschnitte **0a** (DB-Schema Redo), **0b** (SQL-Queries Redo), **0c** (Seed-Daten Redo), **0d** (Doku Redo) vor Abschnitt 1 einfügen (NICHT die bestehenden ✅-Abschnitte 1, 2, 9 überschreiben)
- Abschnitte 3–8: Subject-Format durchziehen (`{nr}/tisch-{id}`), `kassensitzung_state` → `kassensitzungen`
- Subject-Helper-Funktionen: `KassensitzungSubject(zNr int)` statt `(datum string)`, `TischSessionSubject(zNr int, tischID int)` statt `(datum string, tischID int)`, Parsing für `/`-Separator

#### Schritt 13: `docs/agents/kassenjournal-redesign/analyze.md` aktualisieren

- ~11 Stellen: `kassensitzung_state` → `kassensitzungen`
- Subject-Format-Referenzen aktualisieren

---

### Verification

Nach Abschluss aller Schritte:

```bash
# 1. sqlc muss fehlerfrei laufen
make sqlc

# 2. kassensitzung_state darf nur noch in redesign.md §7 und plan.md ✅-Abschnitten vorkommen
grep -rn "kassensitzung_state" --include="*.sql" --include="*.md" .
# Erlaubt: docs/redesign.md (nur §7, ab ~Zeile 930), docs/agents/kassenjournal-redesign/plan.md (✅-Abschnitte)
# NICHT erlaubt: alle anderen Dateien

# 3. Altes Subject-Format darf nur noch in redesign.md §7 vorkommen
grep -rn "kassensitzung-{YYYYMMDD}" --include="*.md" .
# Erlaubt: docs/redesign.md (nur §7)

# 4. Kein -tisch- mehr im Subject-Kontext (nur /tisch-)
grep -rn "kassensitzung-.*-tisch-" --include="*.sql" --include="*.md" .
# Erlaubt: docs/redesign.md §7, docs/agents/kassenjournal-redesign/plan.md ✅-Abschnitte

# 5. Schema-Reihenfolge prüfen: kassensitzungen VOR kassenjournal
grep -n "CREATE TABLE.*kassensitzungen\|CREATE TABLE.*kassenjournal" database/migrations/01_initial.up.sql
# kassensitzungen muss eine KLEINERE Zeilennummer haben als kassenjournal

# 6. Alte Query-Datei ist weg
ls backend/sqlc/queries/kassensitzung_state.sql 2>&1
# Soll: "No such file or directory"

# 7. Neue Query-Datei existiert
ls backend/sqlc/queries/kassensitzungen.sql
# Soll: Datei existiert

# 8. Generierter Code ist aktuell (kein kassensitzung_state.sql.go mehr)
ls backend/sqlc/dbgen/kassensitzung_state.sql.go 2>&1
# Soll: "No such file or directory"
ls backend/sqlc/dbgen/kassensitzungen.sql.go
# Soll: Datei existiert
```

---

### Decisions (Kontext für Rückfragen)

- `kassensitzungen` (Plural) als Tabellenname — konsistent mit `tische`, `produkte`, `users`
- `kassensitzungen` wird direkt VOR `kassenjournal` im Schema eingefügt (FK-Abhängigkeit)
- Neue Redo-Abschnitte 0a/0b/0c/0d im Plan statt Überschreiben von 1+2+9
- `tisch_session_state` bleibt unverändert (Projektion, mit `last_event_id`/`last_event_version`)
- `docs/redesign.md` §7 wird NICHT verändert — es dokumentiert die Entscheidung
- Write-Flow Kassensitzung: INSERT in `kassensitzungen` + INSERT in `kassenjournal` in gleicher TX (bei Eröffnung); UPDATE `kassensitzungen` + INSERT in `kassenjournal` (bei Tagesabschluss)
- `database/seed.sql` ist NICHT betroffen (nutzt altes `events`-Tabellenformat, kein `kassensitzung_state`)

### Betroffene Dateien (vollständige Liste)

**Code (Phase 1):**

- `database/migrations/01_initial.up.sql` — Schema
- `backend/sqlc/queries/kassensitzung_state.sql` — LÖSCHEN
- `backend/sqlc/queries/kassensitzungen.sql` — NEU ERSTELLEN
- `backend/sqlc/dbgen/*` — wird durch `make sqlc` neu generiert

**Dokumentation (Phase 2):**

- `docs/redesign.md` — §3.1–§3.13, ~30 Stellen (§7 NICHT ändern)
- `docs/handbuch.md` — ~20 Stellen
- `docs/language.md` — ~8 Stellen
- `docs/anforderungen.md` — 3 Stellen
- `docs/diagrams.md` — ~20 Stellen
- `docs/adr/event-sourcing.md` — ~15 Stellen
- `docs/adr/cqrs.md` — ~12 Stellen
- `docs/roadmap.md` — 1 Stelle
- `.github/instructions/event-sourcing.instructions.md` — ~6 Stellen
- `.github/instructions/database.instructions.md` — 1 Stelle
- `AGENTS.md` — 2 Stellen
- `README.md` — 1 Stelle

**Plan/Analyse (Phase 3):**

- `docs/agents/kassenjournal-redesign/plan.md` — Redo-Abschnitte + Referenzen
- `docs/agents/kassenjournal-redesign/analyze.md` — 11 Stellen
